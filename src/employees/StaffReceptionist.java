package employees;

public class StaffReceptionist extends StaffUser {
    // Attributes

    public StaffReceptionist(String title, String firstName, String lastName, int phoneNumber, double salary, String userName, String password) {
        super(title, firstName, lastName, phoneNumber, salary, userName, password);
    }

    // getter

    // setter
}