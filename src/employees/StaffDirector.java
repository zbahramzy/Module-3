package employees;

public class StaffDirector extends StaffUser {
    // Attributes

    // Constructor
    public StaffDirector(String title, String firstName, String lastName, int phoneNumber, double salary, String userName, String password) {
        super(title, firstName, lastName, phoneNumber, salary, userName, password);
    }

    // getter

    // setter
}